# ForeVita four week MVP plan

Stakeholder landing page for the ForeVita delivery plan: week-by-week scope, three product surfaces, stack, release gates, and engagement risks.

```bash
npm install
npm run dev
```

Build with `npm run build`. Preview with `npm run preview`.
